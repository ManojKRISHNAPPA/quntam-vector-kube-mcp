[project]
name = "kubernetes-mcp"
version = "1.0.0"
description = "Kubernetes MCP Server for Claude Desktop"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "kubernetes>=29.0.0",
    "boto3>=1.34.0",
    "pyyaml>=6.0",
    "rich>=13.0.0",
]

[tool.uv]
package = false
